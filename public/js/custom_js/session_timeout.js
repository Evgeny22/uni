'use strict';
// initialize session timeout settings
//function closeSession() {
    /*$.sessionTimeout({
        title: 'Session Timeout Notification',
        message: 'Your session is about to expire.',
        keepAliveUrl: window.location.href +'?_token='+ $('meta[name="token"]').val(),
        keepAliveAjaxRequestType: 'GET',
        redirUrl: APP_URL +'/logout',
        logoutUrl: APP_URL +'/logout',
        warnAfter: 5000,
        redirAfter: 10000
    });*/
/*
}
jQuery(document).ready(function () {

    var timeoutid = 0;

    $("html").mousemove(function() {
        clearTimeout(timeoutid);
        timeoutid = setTimeout(closeSession, 150000);
    });

    var left_height = $(window).height() - 1;
    $('.wrapper').css('height', left_height);
});*/
